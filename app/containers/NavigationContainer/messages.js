/*
 * NavigationContainer Messages
 *
 * This contains all the text for the NavigationContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NavigationContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavigationContainer container!',
  },
});
